name: Coffee Scraper & Builder Pipeline

on:
  # Run daily at 6 AM UK time
  schedule:
    - cron: '0 6 * * *'

  # Allow manual triggering
  workflow_dispatch:

  # Also run on push to main to pick up code changes immediately
  push:
    branches:
      - main
    paths:
      - 'scraper/**'
      - 'builder/**'
      - 'common/**'

jobs:
  scrape:
    name: Scrape Coffee Data
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      # Disable caching to ensure code changes are always reflected
      # The build only takes ~2 minutes so caching isn't worth the debugging headaches

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Run scraper
        working-directory: scraper
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        run: |
          echo "🚀 Starting coffee scraper..."
          cargo run --release --bin coffee-scraper
          echo "✅ Scraping complete"

  build:
    name: Build & Deploy Static Site
    runs-on: ubuntu-latest
    needs: scrape

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      # Disable caching to ensure code changes are always reflected

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Run builder
        working-directory: builder
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          REPO_URL: github.com/nikulpatel3141/CoffeeAggregatorWebsite
          TARGET_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.BUILDER_GITHUB_TOKEN }}
        run: |
          echo "🏗️ Starting static site builder..."
          cargo run --release
          echo "✅ Build and deploy complete"

      - name: Summary
        run: |
          echo "### ✅ Pipeline Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Coffee data scraped and stored in Firestore" >> $GITHUB_STEP_SUMMARY
          echo "- Static site built and pushed to CoffeeAggregatorWebsite repo" >> $GITHUB_STEP_SUMMARY
          echo "- Vercel will auto-deploy the updates" >> $GITHUB_STEP_SUMMARY
